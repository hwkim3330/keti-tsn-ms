#!/bin/sh
# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# Change to mvdct directory so it can find its resources
cd "$SCRIPT_DIR"
# Use self-contained binary from publish directory
"$SCRIPT_DIR/publish/mvdct" "$@"
echo
